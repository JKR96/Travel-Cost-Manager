<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.radziwinski.jakub.travelcostmanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/NoActionBarTheme">
        <activity android:name=".controller.LoadingActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.AddTripActivity" />
        <activity
            android:name=".controller.SingleTripActivity"
            android:label="@string/title_activity_single_trip"
            android:theme="@style/NoActionBarTheme" />
        <activity
            android:name=".controller.TripListActivity"
            android:label="@string/title_activity_trip_list"
            android:theme="@style/NoActionBarTheme" />
    </application>

</manifest>